<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Détail apprenant</title>

  <style>
    body {
      font-family: Arial;
      background: #f4f6f9;
      padding: 40px;
    }

    .card {
      max-width: 600px;
      margin: auto;
      background: white;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 5px 20px rgba(0,0,0,0.1);
    }

    h1 {
      text-align: center;
      color: #333;
    }

    .info {
      margin: 10px 0;
      font-size: 18px;
    }

    .label {
      font-weight: bold;
      color: #555;
    }

    a {
      display: inline-block;
      margin-top: 20px;
      text-decoration: none;
      background: black;
      color: white;
      padding: 10px 15px;
      border-radius: 6px;
    }
  </style>
</head>

<body>

  <div class="card">
    <h1>Détail de l'apprenant</h1>

    <div class="info"><span class="label">Nom :</span> {{ apprenant.name }}</div>
    <div class="info"><span class="label">Prénom :</span> {{ apprenant.lastName }}</div>
    <div class="info"><span class="label">Email :</span> {{ apprenant.email }}</div>
    <div class="info"><span class="label">Genre :</span> {{ apprenant.genre }}</div>
    <div class="info"><span class="label">Téléphone :</span> {{ apprenant.phone }}</div>

    <a href="/apprenants">← Retour à la liste</a>
  </div>

</body>
</html>
